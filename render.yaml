services:
  - type: web
    name: the-quiet-game
    runtime: python
    region: oregon
    plan: free
    branch: deployment
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
